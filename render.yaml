services:
  - type: web
    name: vaultnote-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: APP_ENV
        value: production
    disk:
      name: vaults
      mountPath: /var/www/vaults
      sizeGB: 10